//collect all prime numbers into an array
//at the bottom of this file is something called a function, which can take any positive number greater than 1, and return a boolean, telling you whether that number is prime or not.
//for example: isPrime(5) will be true. isPrime(10) will be false

let someNumbers = [5125, 1223, 9481, 2984, 112341, 95819, 737593]
let primeNumbers = []





console.log(primeNumbers)

let isPrime = function(number) {
    while (i < maybePrimeNumber) {
        if (maybePrimeNumber % i === 0 ) return false
    }
    return true 
}